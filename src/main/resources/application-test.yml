spring:
  datasource:
    url: jdbc:h2:mem:hislink_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

security:
  jwt:
    secret: test-secret-key-test-secret-key-123
    access-token-expire-ms: 3600000
