spring:
  application:
    name: hislink-be
  profiles:
    active: local
    group:
      local: local
      deploy: deploy

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
